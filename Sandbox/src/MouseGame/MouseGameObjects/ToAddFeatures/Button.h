#pragma once

#include <GT.h>

namespace MouseGameLayer {

	/*class Button : public GT::Component::ElementInContainer<Button, GT::Component::Positional, GT::Component::ObjectRenderable>
	{
	public:
		Button() = default;
		void Init(GT::Vector2<float> position, GT::Vector2<float> size, float depth, const GT::SpriteSheet::InitializerForSpriteSheet& spriteSheetData, std::function<void()> onClick);

		// Eliminates handled clicks
		void OnUpdate(Hazel::Timestep ts, std::vector<GT::Vector2<float>>& clicks);
		void OnRender();
	private:
		std::function<void()> m_OnClick = nullptr;
	};*/

}